import * as React from 'react';
import styled from 'styled-components';

export const PlaceholderTicker = () => {
  return <StyledPlaceholderTicker />;
};

const StyledPlaceholderTicker = styled.div`
  height: 100px;
  background-color: pink;
`;
