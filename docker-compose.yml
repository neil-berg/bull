version: '3'
services:
  server:
    container_name: bull-api
    restart: always
    build: ./server
    ports:
      - '3000:3000'
    depends_on:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'
